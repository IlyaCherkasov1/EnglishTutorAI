export const publicRoutes = [
    "/public/route"
];

export const LOGIN_PATH = "/auth/login";
const REGISTER_PATH = "/auth/register";

export const authRoutes = [LOGIN_PATH, REGISTER_PATH];
export const DEFAULT_LOGIN_REDIRECT_URI = "/";